/*
 * Operadores binarios:
 * + Suma dos valores, en caso de ser dos tipos distintos siendo uno un string, los concatena
 * - Resta
 * * Multiplica
 * / Divide
 *
 *
 * Operador de asignación
 * = Asigna un valor
 *
 *
 * Operadores de comparación:
 * == Compara el contenido de dos valores, si el tipo es distinto no importa
 * === Compara el contenido de dos valores y el tipo, de forma estricta.
 * < Compara dos numeros, si el primero es menor que el segundo es true
 * > Compara dos números, si el primero es mayor que el segundo es true
 * <= Compara dos valores, si el primero es menor o igual al segundo, es true
 * >= Compara dos valores, si el primero es mayor o igual al segundo, es true
 *
 *
 * Operadores lógicos:
 * ! Negación, niega lo que le sigue (!true -> false)
 * && Signo de puerta lógica AND
 * || Signo de puerta lógica OR
 *
 * Operadores incrementales:
 * ++ Incrementa el valor en uno, si es antes del valor, se añade antes de la asignación, si no, después
 * += Incrementa con el valor que se indica después el valor de lo que le precede
 *
 */
